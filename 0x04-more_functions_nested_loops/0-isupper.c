#include "main.h"
/**
 * _isupper - letter upper
 * @x: num checked
 * Return: 1 for upper or 0 for anything else
 */
int _isupper(int x)

{
	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}
